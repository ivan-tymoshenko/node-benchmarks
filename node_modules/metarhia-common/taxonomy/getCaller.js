/*const getCaller = () => {
  const handler = Error.prepareStackTrace;
  Error.prepareStackTrace = function(err, stack) {
    Error.prepareStackTrace = handler;
    return stack;
  };
  const stack = new Error().stack;

  let line, fn;
  do {
    line = stack.shift();
    fn = line.getFunctionName();
    console.dir({ fn });
  } while (stack.length);

  return fn;
};*/
